# 按顺序为槽指定参数值
print("() {) {". format(1, 2, 3))
# 槽里面有名称，按照名称匹配参数值
print("ix) iy) (z}". format(x=1, z=2, v=3))
# 槽中指定了参数的序号，按照序号对应参数替换
print("{0}的{2子成绩是：111分".format("小明", 95, "语文"))
# 运行结果：小明的语文成绩是：95分
